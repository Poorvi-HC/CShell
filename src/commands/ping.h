#ifndef PING_H
#define PING_H

// include custom header files
#include "../definitions.h"
#include "../utilities.h"

// function declarations
int ping(char* args[]);
int runPing(char* args[]);

#endif